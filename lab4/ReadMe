To compile:
make

To run:
./comp < inputfile

After compiling, 3 files will be generated:
* ast.dot [Contains nodes information formatted for dot to be graphed]
* cfg.dot [Contains nodes information formatted for dot to be graphed]
* instructions.s [Contains instructions information]

To get the pdf file of AST:
dot -Tpdf ast.dot -o ast.pdf

To get the pdf file of CFG:
dot -Tpdf cfg.dot -o cfg.pdf

## KNOWN ISSUES:
* somtimes the instructions in the join block is lost [Fixed in Update 1.2.x]
* In 'print' and 'eval' the first child/grand_child node is missing [Fixed in Update 1.1]
